# Archivo de configuración para producción
# Debe ser renombrado a .env y configurado con valores reales

# Base de datos
POSTGRES_PASSWORD=tu_password_super_seguro_aqui
DATABASE_URL=postgresql://postgres:tu_password_super_seguro_aqui@postgres:5432/fciencias_app

# JWT Secrets (generar con openssl rand -base64 64)
JWT_SECRET=tu_jwt_secret_muy_largo_y_seguro_aqui_64_caracteres_minimo
JWT_REFRESH_SECRET=tu_jwt_refresh_secret_diferente_y_seguro_aqui_64_caracteres

# URLs
FRONTEND_URL=https://fciencias.app
NEXT_PUBLIC_API_URL=https://api.fciencias.app/api/v1

# Email configuración (opcional para futuro)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=tu_email@gmail.com
# SMTP_PASSWORD=tu_app_password

# Configuración adicional
NODE_ENV=production
CORS_ORIGIN=https://fciencias.app
